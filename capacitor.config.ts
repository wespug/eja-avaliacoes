import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eja.avaliacoes',
  appName: 'eja-avaliacoes',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
